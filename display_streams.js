$(document).ready(function () {
    
    $.getJSON("./twitchsampleAPI.json", function(json) {
        var html="";

        // For each stream, recreate div depending on the value of "stream"

        // $("#stream-table-body").html(html);
    });
    
});